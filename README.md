# chip8-emulator

Simple CHIP-8 CPU emulator. Can currently run the IBM Logo test rom.

## Install and run

```shell
cargo install
```

```shell
cargo run "IBM Logo.ch8"
```

tests

```shell
cargo test
```
